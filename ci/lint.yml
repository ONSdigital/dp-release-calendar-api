---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: latest

inputs:
  - name: dp-release-calendar-api

run:
  path: dp-release-calendar-api/ci/scripts/lint.sh